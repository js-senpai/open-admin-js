{
  "name": "@openadminjs/example-blog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Reference code: authors, tags, and post↔tag join for an editorial stack.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@openadminjs/core": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
