{
  "name": "@openadminjs/plugin-sdk",
  "version": "0.1.0",
  "description": "Contracts for OpenAdminJS server plugins (hooks, manifest metadata).",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@openadminjs/core": "workspace:*"
  },
  "scripts": {
    "build": "echo \"@openadminjs/plugin-sdk ships TypeScript sources; emit dist when publishing to npm\"",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.2.0"
  }
}
