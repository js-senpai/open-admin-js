{
  "name": "create-openadminjs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "create-openadminjs": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../../scripts/make-executable.mjs dist/index.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "^0.8.2",
    "fs-extra": "^11.2.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
